---
// No imports ni lógica extra - solo en español

// Datos de los pisos en español
const flooringData = [
  {
    key: 'Vinyl',
    label: 'Pisos de Vinilo',
    image: 'https://res.cloudinary.com/drwacbtjf/image/upload/v1743508300/WhatsApp_Image_2025-03-31_at_23.10.44_ff4b6490_phzxzy.jpg',
    bgImage: 'https://res.cloudinary.com/drwacbtjf/image/upload/v1744642268/vynil_sie1yd.jpg',
    gallery: [
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1744662421/vinyll_samvcc.jpg',
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1743508300/WhatsApp_Image_2025-03-31_at_23.10.44_ff4b6490_phzxzy.jpg',
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1744642268/vynil_sie1yd.jpg'
    ]
  },
  {
    key: 'Laminate',
    label: 'Pisos Laminados',
    image: 'https://res.cloudinary.com/drwacbtjf/image/upload/v1743508301/WhatsApp_Image_2025-03-31_at_23.10.44_86077beb_mnfxib.jpg',
    bgImage: 'https://res.cloudinary.com/drwacbtjf/image/upload/v1744642091/laminate_1_ewkfrn.jpg',
    gallery: [
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1744643166/Imagen_de_WhatsApp_2025-02-06_a_las_18.38.14_faf1dc6f_l12fpq.jpg',
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1743508301/WhatsApp_Image_2025-03-31_at_23.10.44_86077beb_mnfxib.jpg',
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1744642091/laminate_1_ewkfrn.jpg'
    ]
  },
  {
    key: 'Tile',
    label: 'Azulejos y Porcelanatos',
    image: 'https://res.cloudinary.com/drwacbtjf/image/upload/v1743508301/WhatsApp_Image_2025-03-31_at_23.10.45_7e4276ce_gettwe.jpg',
    bgImage: 'https://res.cloudinary.com/drwacbtjf/image/upload/v1744642267/Tile_tbbln5.jpg',
    gallery: [
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1744643216/Imagen_de_WhatsApp_2025-02-06_a_las_18.37.00_44592a86_jeumna.jpg',
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1743508301/WhatsApp_Image_2025-03-31_at_23.10.45_7e4276ce_gettwe.jpg',
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1744642267/Tile_tbbln5.jpg'
    ]
  },
  {
    key: 'Wood',
    label: 'Pisos de Madera',
    image: 'https://res.cloudinary.com/drwacbtjf/image/upload/v1743508300/WhatsApp_Image_2025-03-31_at_23.10.44_3f250b6b_g2xic4.jpg',
    bgImage: 'https://res.cloudinary.com/drwacbtjf/image/upload/v1744642270/wood_kctkbz.jpg',
    gallery: [
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1744643154/Imagen_de_WhatsApp_2025-02-06_a_las_18.37.00_737de658_ioa8xa.jpg',
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1743508300/WhatsApp_Image_2025-03-31_at_23.10.44_3f250b6b_g2xic4.jpg',
      'https://res.cloudinary.com/drwacbtjf/image/upload/v1744642270/wood_kctkbz.jpg'
    ]
  }
];

// Contenido de los modales
const vinylContent = [
  {
    title: "Durabilidad Excepcional",
    description: "Los pisos de vinilo son extremadamente resistentes al desgaste, rayones y manchas. Perfectos para áreas de alto tráfico como cocinas, baños y entradas."
  },
  {
    title: "Resistente al Agua",
    description: "Ideal para cocinas, baños y sótanos. No se hincha ni se deforma con la humedad, manteniendo su belleza por años."
  },
  {
    title: "Fácil Mantenimiento",
    description: "Solo requiere limpieza regular con agua y jabón suave. No necesita cera ni selladores especiales."
  },
  {
    title: "Variedad de Estilos",
    description: "Disponible en una amplia gama de colores, texturas y patrones que imitan madera, piedra o diseños modernos."
  }
];

const laminateContent = [
  {
    title: "Apariencia Natural",
    description: "Imita perfectamente la apariencia de la madera real con texturas y patrones auténticos a una fracción del costo."
  },
  {
    title: "Instalación Sencilla",
    description: "Sistema de clic que permite instalación rápida y sin adhesivos. Ideal para proyectos de bricolaje o profesionales."
  },
  {
    title: "Resistente al Desgaste",
    description: "Capa superior de protección que resiste rayones, manchas y desgaste diario, manteniendo su belleza por años."
  },
  {
    title: "Económico",
    description: "Ofrece la elegancia de la madera natural a un precio accesible, perfecto para presupuestos limitados."
  }
];

const tileContent = [
  {
    title: "Durabilidad Máxima",
    description: "Los azulejos y porcelanatos son los materiales más duraderos disponibles, resistiendo décadas de uso intenso."
  },
  {
    title: "Resistente a Todo",
    description: "Impermeable, resistente al fuego, rayones y productos químicos. Ideal para cualquier ambiente del hogar."
  },
  {
    title: "Infinita Variedad",
    description: "Disponible en todos los colores, tamaños, texturas y patrones imaginables para crear espacios únicos."
  },
  {
    title: "Valor Agregado",
    description: "Aumenta significativamente el valor de su propiedad y es una inversión que dura toda la vida."
  }
];

const woodContent = [
  {
    title: "Belleza Natural",
    description: "Cada tabla es única con vetas y colores naturales que crean un ambiente cálido y elegante en cualquier espacio."
  },
  {
    title: "Durabilidad Probada",
    description: "Con el cuidado adecuado, los pisos de madera pueden durar generaciones, mejorando con el tiempo."
  },
  {
    title: "Versatilidad",
    description: "Disponible en múltiples especies, colores y acabados para adaptarse a cualquier estilo decorativo."
  },
  {
    title: "Inversión Inteligente",
    description: "Aumenta el valor de su propiedad y es una opción sostenible que mejora la calidad del aire interior."
  }
];
---

<section id="flooring-section" data-aos="fade-up">
  <div class="flooring-sections" data-aos="zoom-in" data-aos-delay="200">
    <div id="flooring-bg-overlay" class="flooring-bg-overlay"></div>
    <h2 class="flooring-title" data-aos="fade-up" data-aos-delay="100">Nuestros Tipos de Pisos</h2>
    <div class="flooring-grid" data-aos="fade-up" data-aos-delay="300">
      {flooringData.map((floor, index) => (
        <div
          class="flooring-card"
          data-floor={floor.key}
          data-bg-image={floor.bgImage}
          data-gallery={JSON.stringify(floor.gallery)}
          data-aos="fade-up"
          data-aos-delay={400 + index * 100}
        >
          <img src={floor.image} alt={floor.label} />
          <div class="floor-label">{floor.label}</div>
        </div>
      ))}
    </div>
  </div>

  <!-- Contenido oculto para cada tipo de piso -->
  <div id="vinyl-content" style="display: none;">
    {vinylContent.map((item, index) => (
      <div>
        <h4>{item.title}</h4>
        <p>{item.description}</p>
      </div>
    ))}
  </div>

  <div id="laminate-content" style="display: none;">
    {laminateContent.map((item, index) => (
      <div>
        <h4>{item.title}</h4>
        <p>{item.description}</p>
      </div>
    ))}
  </div>

  <div id="tile-content" style="display: none;">
    {tileContent.map((item, index) => (
      <div>
        <h4>{item.title}</h4>
        <p>{item.description}</p>
      </div>
    ))}
  </div>

  <div id="wood-content" style="display: none;">
    {woodContent.map((item, index) => (
      <div>
        <h4>{item.title}</h4>
        <p>{item.description}</p>
      </div>
    ))}
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    let selectedFloor = null;
    let bgImage = null;
    let fadeOut = false;
    let fixedBgImage = null;

    const flooringBgOverlay = document.getElementById('flooring-bg-overlay');
    const flooringCards = document.querySelectorAll('.flooring-card');

    function updateBackground() {
      if (!flooringBgOverlay) return;
      
      const backgroundImage = fixedBgImage || bgImage;
      if (backgroundImage) {
        flooringBgOverlay.style.backgroundImage = `url(${backgroundImage})`;
        flooringBgOverlay.classList.toggle('fade-out', fadeOut && !fixedBgImage);
      } else {
        flooringBgOverlay.style.backgroundImage = 'none';
      }
    }

    flooringCards.forEach(card => {
      card.addEventListener('click', () => {
        const floorKey = card.getAttribute('data-floor');
        const galleryData = card.getAttribute('data-gallery');
        if (floorKey && galleryData) {
          selectedFloor = floorKey;
          fixedBgImage = card.getAttribute('data-bg-image');
          updateBackground();
          
          // Crear el modal con galería
          const modal = document.createElement('flooring-modal');
          modal.setAttribute('gallery', galleryData);
          document.body.appendChild(modal);
          
          // Mostrar información del piso
          const contentElement = document.getElementById(`${floorKey.toLowerCase()}-content`);
          if (contentElement) {
            const content = contentElement.innerHTML;
            const infoModal = document.createElement('div');
            infoModal.className = 'info-modal';
            infoModal.innerHTML = `
              <div class="info-content">
                <h2>${floorKey}</h2>
                <div class="info-text">
                  ${content}
                </div>
                <button class="close-btn">Cerrar</button>
              </div>
            `;
            document.body.appendChild(infoModal);
            
            const closeBtn = infoModal.querySelector('.close-btn');
            closeBtn?.addEventListener('click', () => {
              infoModal.remove();
            });
          }
        }
      });

      card.addEventListener('mouseenter', () => {
        bgImage = card.getAttribute('data-bg-image');
        fadeOut = false;
        updateBackground();
      });

      card.addEventListener('mouseleave', () => {
        fadeOut = true;
        updateBackground();
      });
    });
  });
</script>

<style>
.flooring-sections {
  position: relative;
  overflow: hidden;
  text-align: center;
  padding: 40px 20px;
  background-color: #ffffff;
}

.flooring-title {
  font-size: 2.2rem;
  margin-bottom: 20px;
  color: rgba(42, 120, 146, 0.7);
  display: inline-block;
  padding: 5px 10px;
  background-color: rgba(240, 248, 255, 0.5);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);  
  border-radius: 5px;
}

.flooring-bg-overlay {
  position: absolute;
  inset: 0;
  background-size: cover;
  background-position: center;
  opacity: 1;
  transition: opacity 0.8s ease-in-out;
  z-index: 0;
}

.flooring-bg-overlay.fade-out {
  opacity: 0;
}

.flooring-sections > *:not(.flooring-bg-overlay) {
  position: relative;
  z-index: 1;
}

.flooring-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  margin: 40px 0;
}

.flooring-card {
  cursor: pointer;
  position: relative;
  overflow: hidden;
  border-radius: 12px;
  box-shadow: 0 4px 16px rgba(0,0,0,0.1);
  transition: transform 0.3s ease;
}

.flooring-card:hover {
  transform: translateY(-5px);
}

.flooring-card img {
  width: 100%;
  height: auto;
  display: block;
  object-fit: contain;
  transition: transform 0.6s cubic-bezier(0.25, 0.1, 0.25, 1), opacity 0.6s ease-in-out;
}

.flooring-card:hover img {
  transform: scale(1.07);
  opacity: 0.9;
}

.floor-label {
  position: absolute;
  bottom: 10px;
  left: 10px;
  color: #fff;
  font-weight: bold;
  background-color: rgba(0, 0, 0, 0.5);
  padding: 5px 10px;
  border-radius: 5px;
}

.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  min-height: 100vh;
  background: rgba(0, 0, 0, 0.5);
  overflow-y: auto;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  padding: 20px 0;
  z-index: 1000;
}

.modal-card-two-columns {
  display: flex;
  flex-direction: row;
  background: #fff;
  border-radius: 15px;
  overflow: hidden;
  width: 90%;
  max-width: 1000px;
  max-height: 90vh;
}

.modal-image-column {
  width: 50%;
  overflow: hidden;
}

.modal-image-column img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  pointer-events: none;
}

.modal-text-column {
  width: 50%;
  padding: 20px;
  overflow-y: auto;
}

.modal-title {
  font-size: 1.8rem;
  margin-bottom: 20px;
  color: rgba(42, 120, 146, 0.7);
}

.modal-content-scroll {
  overflow-y: auto;
  padding: 20px 20px;
}

.modal-content-scroll h4 {
  color: #2a7892;
  margin-bottom: 10px;
  font-size: 1.2rem;
}

.modal-content-scroll p {
  margin-bottom: 16px;
  line-height: 1.6;
  color: #444;
}

.close-btn {
  background-color: #8bbaca;
  border: none;
  padding: 10px 20px;
  margin-top: 20px;
  cursor: pointer;
  border-radius: 5px;
  display: block;
  margin-left: auto;
  margin-right: auto;
  color: black;
  font-weight: 600;
  transition: background-color 0.3s ease;
}

.close-btn:hover {
  background-color: #7aa8b8;
}

@media (max-width: 768px) {
  .modal-overlay {
    padding: 10px 0;
    align-items: flex-start;
  }

  .modal-card-two-columns {
    flex-direction: column;
    width: 95%;
    max-height: 90vh;
    border-radius: 10px;
    position: relative;
  }

  .modal-image-column {
    width: 100%;
    max-height: 40vh;
  }
  
  .modal-image-column img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    border-radius: 10px 10px 0 0;
  }

  .modal-text-column {
    width: 100%;
    padding: 20px;
    text-align: left;
    max-height: 50vh;
    overflow-y: auto;
    position: relative;
  }

  .modal-content-scroll {
    max-height: calc(50vh - 120px);
    overflow-y: auto;
    padding: 0 0 20px 0;
  }

  .modal-title {
    font-size: 1.6rem;
    text-align: center;
    background-color: rgba(255, 255, 255, 0.8);
    padding: 10px;
    border-radius: 10px;
    margin-bottom: 20px;
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .close-btn {
    position: sticky;
    bottom: 0;
    width: 100%;
    margin-top: 20px;
    background-color: #8bbaca;
    padding: 12px;
    font-size: 1.1rem;
    border-radius: 10px;
    color: black;
    z-index: 10;
  }
}

button,
button:focus,
button:active,
a,
a:focus,
a:active,
.flooring-card,
.flooring-card:focus,
.flooring-card:active,
.close-btn,
.close-btn:focus,
.close-btn:active {
  outline: none !important;
  box-shadow: none !important;
  -webkit-tap-highlight-color: transparent !important;
}

.info-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: white;
  border-radius: 15px;
  padding: 2rem;
  max-width: 500px;
  width: 90vw;
  max-height: 80vh;
  overflow-y: auto;
  z-index: 1060;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

.info-content h2 {
  color: #2a7892;
  margin-bottom: 1rem;
  font-size: 1.8rem;
}

.info-text h4 {
  color: #2a7892;
  margin: 1rem 0 0.5rem 0;
  font-size: 1.2rem;
}

.info-text p {
  margin-bottom: 1rem;
  line-height: 1.6;
  color: #444;
}

.info-modal .close-btn {
  background-color: #8bbaca;
  border: none;
  padding: 10px 20px;
  margin-top: 1rem;
  cursor: pointer;
  border-radius: 5px;
  color: black;
  font-weight: 600;
  width: 100%;
}
</style> 